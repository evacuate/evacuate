{
  "healthchecks": {
    "web": [
      {
        "type": "startup",
        "name": "web check",
        "description": "Checking if the app responds to the /health endpoint",
        "path": "/health",
        "attempts": 3,
        "port": 3000,
        "initialDelay": 10
      }
    ]
  }
}
